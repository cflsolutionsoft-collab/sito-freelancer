import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // URL coerenti: /servizi/ (con trailing slash)
  trailingSlash: true,

  // Headers di caching per asset statici
  async headers() {
    return [
      {
        source: "/images/:path*",
        headers: [
          {
            key: "Cache-Control",
            value: "public, max-age=31536000, immutable",
          },
        ],
      },
      {
        source: "/favicon.ico",
        headers: [
          {
            key: "Cache-Control",
            value: "public, max-age=31536000, immutable",
          },
        ],
      },
    ];
  },
};

export default nextConfig;
